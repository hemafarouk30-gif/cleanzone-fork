module {
  public type UserId = Principal;
  public type Timestamp = Int;
  public type ReportId = Nat;
  public type MessageId = Nat;
  public type ChatId = Text; // canonical: sorted principal pair joined by ":"
};
